const c=document.getElementById("bg"),x=c.getContext("2d");let p=[];function rs(){c.width=innerWidth*devicePixelRatio;c.height=innerHeight*devicePixelRatio;c.style.width=innerWidth+"px";c.style.height=innerHeight+"px";x.setTransform(devicePixelRatio,0,0,devicePixelRatio,0,0);p=Array.from({length:Math.min(180,Math.floor(innerWidth/8))},(_,i)=>({x:Math.random()*innerWidth,y:Math.random()*innerHeight,vx:(Math.random()-.5)*.35,vy:(Math.random()-.5)*.35,r:Math.random()*1.7+.3,col:i%5===0?"#9666ff":i%2?"#4ad7ff":"#ff436d"}))}addEventListener("resize",rs);rs();(function loop(){x.clearRect(0,0,innerWidth,innerHeight);for(const a of p){a.x+=a.vx;a.y+=a.vy;if(a.x<0||a.x>innerWidth)a.vx*=-1;if(a.y<0||a.y>innerHeight)a.vy*=-1;x.fillStyle=a.col;x.globalAlpha=.45;x.beginPath();x.arc(a.x,a.y,a.r,0,Math.PI*2);x.fill()}x.globalAlpha=1;requestAnimationFrame(loop)})();